image: node:10.1.0

cache:
  paths:
  - node_modules/
  - .yarn

stages:
  - build
  - test

before_script:
  - yarn config set cache-folder .yarn
  - yarn install

build:
  stage: build
  script: yarn run build

test:
  stage: test
  script: yarn test
